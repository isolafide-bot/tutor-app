name: tutor_app
description: 과외 선생님 전용 관리 앱
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # 아래 세 줄이 핵심 도구들입니다.
  intl: ^0.18.1
  shared_preferences: ^2.2.0
  url_launcher: ^6.1.11
  cupertino_icons: ^1.0.5

flutter:
  uses-material-design: true
